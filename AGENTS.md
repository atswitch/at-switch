# AT-Switch 开发与协作规范

本文件适用于仓库根目录及全部子目录。所有开发者和自动化编码 Agent 在修改代码、
测试、文档、构建或发布配置前，都必须遵守本规范。

## 1. 指令与事实来源

发生冲突时按以下优先级处理：

1. 用户在当前任务中的明确要求；
2. 仓库根目录或更深目录中的 `AGENTS.md`；
3. 可执行代码、自动化测试和数据库迁移所体现的现有行为；
4. `docs/` 下的架构、协议、产品和发布文档；
5. README、使用教程和历史说明。

开始开发前必须阅读相关代码、测试和文档，并检查 Git 工作区。不得覆盖、还原、
移动或删除与当前任务无关的用户修改。需求存在歧义时，先完成不依赖该歧义的调查
和可逆工作；涉及不可逆行为、数据语义变化或多个合理方案且会显著影响结果时，再向
用户确认。

## 2. 项目目标与基本约束

AT-Switch 为 macOS 和 Windows 上的 AI Agent 提供统一的 Provider、模型和接入方式
切换能力。所有实现必须满足：

- 本地优先：核心功能不依赖 AT-Switch 自有云服务；
- 双平台一致：macOS 与 Windows 共享业务流程、领域状态和错误语义；
- 配置安全：写入前备份，写入后校验，失败自动回滚；
- Secret 隔离：真实 API Key 只进入系统凭据库或目标 Agent 明确要求的配置位置；
- 尊重用户配置：只修改 AT-Switch 管理的字段，保留未知字段、插件、注释和登录态；
- 行为可验证：新增行为必须有有效的自动化测试或明确的真机验收记录；
- 向后兼容：新增 Agent 或 Provider 不得改变现有 Agent 的默认行为。

## 3. 开源标准

所有进入仓库的内容必须能够被公开审查、复现和安全分发。

### 3.1 代码与仓库卫生

- 不提交 API Key、密码、访问令牌、Cookie、账号 ID、个人信息、客户数据或真实请求
  内容；
- 不提交本机绝对路径、开发者用户名、内网地址、临时脚本、调试日志、数据库、缓存、
  测试会话、安装包或构建目录；
- 不提交 `node_modules/`、`dist/`、`src-tauri/target/` 和操作系统生成文件；
- 示例、测试和 fixture 只能使用明显虚构的数据与 Secret；
- 不保留已废弃实现、注释掉的大段代码、无调用分支、临时兼容开关或无法解释的补丁；
- 注释解释原因、约束和兼容背景，不复述代码表面动作；
- 公共错误必须使用稳定错误码，并提供可执行的恢复建议；
- 用户可见文案、README、测试和实际行为必须一致。

### 3.2 许可证、依赖与第三方资产

- 新增依赖前先确认必要性、许可证、维护状态、Rust MSRV、Node 版本、安装包体积和安全
  影响；能够使用标准库或现有依赖完成时，不新增依赖；
- 依赖变化必须同步 lockfile，并通过 `npm run licenses:check`；
- 第三方名称、图标、商标和素材必须记录准确来源、用途与权利说明，并同步更新
  `THIRD_PARTY_NOTICES.md` 和相应资产说明；
- 不从网络或本机安装包提取素材后直接提交，除非用户明确要求且已补充来源与分发说明；
- 不修改或移除仓库许可证、版权声明和第三方声明，除非任务明确要求并已核实依据。

### 3.3 可复现性与可移植性

- 文档命令必须从仓库根目录可执行，路径使用仓库相对路径；
- 路径通过 `Path`、`PathBuf`、`DiscoveryContext` 和系统目录 API 动态解析；
- 不写死用户名、盘符、端口或仅在单台机器成立的路径；
- 平台能力未经对应平台构建或真机验证时，必须准确说明验证边界，不得宣称已经通过；
- 对外行为变化需要同步 README、设计文档、使用教程或 CHANGELOG 中实际受影响的部分。

## 4. SOLID 与新功能开发原则

新增功能必须优先扩展现有抽象，控制修改范围，并遵守 SOLID。

### 4.1 单一职责原则（SRP）

- React 页面负责编排用户流程，组件负责展示与交互，`src/lib/` 负责纯能力判断和转换；
- Tauri Command 只接收参数、调用服务并返回结构化结果；
- Agent 适配器只负责该 Agent 的发现、协议约束、配置生成、校验与恢复；
- 配置事务、Secret、数据库、生命周期和协议转换分别由现有专用模块负责；
- 不把平台发现、文件写入、Provider 请求和 UI 状态塞进同一个函数或模块。

### 4.2 开闭原则（OCP）

- 新增 Agent 应新增独立 `AgentAdapter` 并注册到 `AgentRegistry`，不得复制或改写其他
  Agent 的业务实现；
- 新增协议能力应扩展领域类型、能力映射和 Codec，不在页面散布 Agent 名称判断；
- 公共流程通过 trait、注册表和能力函数扩展，避免持续扩大硬编码白名单；
- 为新类型增加实现时，现有类型的默认行为和测试结果必须保持不变。

### 4.3 里氏替换原则（LSP）

- 每个 `AgentAdapter` 必须遵守相同的检测、生成、校验、恢复和错误契约；
- 适配器返回“健康”或“已应用”时，磁盘配置必须确实满足对应状态；
- 不通过特殊返回值、隐藏副作用或跳过校验破坏调用方假设。

### 4.4 接口隔离原则（ISP）

- 只实现 Agent 实际支持的协议和生命周期能力；
- 不支持的直连、写入或重启组合必须在执行前返回准确的结构化错误；
- 不为了单个 Agent 给所有适配器增加无关参数或空实现；确需扩展公共接口时，先评估
  是否应拆成更小的能力接口。

### 4.5 依赖倒置原则（DIP）

- 业务逻辑依赖 `AgentAdapter`、SecretStore、配置事务等抽象，不直接依赖具体操作系统
  或 UI；
- 文件系统、系统凭据、进程和网络等外部能力通过现有基础设施层注入或封装；
- 测试使用临时目录、内存数据库和内存凭据库，不依赖开发者真实环境。

### 4.6 最小变更原则

- 优先修复根因，不用 UI 隐藏、环境变量注入或安装包修改掩盖问题；
- 不夹带与任务无关的重构、格式化、依赖升级、版本升级或文案调整；
- 删除代码前必须确认它是本次引入的冗余或已被证明无调用；
- 版本号只有在用户明确要求发布版本或任务本身是版本发布时才修改。

## 5. 项目架构边界

### 5.1 前端

- `src/pages/`：页面布局和用户流程；
- `src/components/`：可复用组件和业务表单；
- `src/lib/api.ts`：Tauri IPC 边界与浏览器 Mock；
- `src/lib/agentCapabilities.ts`：前端 Agent 能力展示；
- `src/types.ts`：与 Rust 序列化结构一致的前端契约；
- `src/styles.css`：全局视觉规则。

前端不得直接读写 Agent 配置、数据库或系统凭据。所有原生操作必须通过 Tauri
Command 调用 Rust 服务。

### 5.2 Rust 核心

- `src-tauri/src/domain/`：领域类型、状态和可恢复错误；
- `src-tauri/src/services/`：Provider 服务与配置事务；
- `src-tauri/src/agents/`：Agent 注册、发现、适配和生命周期；
- `src-tauri/src/proxy/`：本地代理、路由与协议 Codec；
- `src-tauri/src/infrastructure/`：SQLite 与系统凭据库；
- `src-tauri/src/commands.rs`：轻量 IPC 适配层。

业务规则不得堆积在 `commands.rs`、React 页面或 Mock 数据中。

## 6. 新 Agent 接入规范

新增 Agent 前必须先验证真实运行机制，不能只依据文件名或一次成功请求推断。

### 6.1 调研与设计

至少确认以下事实：

1. macOS 应用名、Bundle Identifier、进程名和标准安装位置；
2. Windows 可执行文件名、标准安装位置及用户数据目录；
3. 活跃账号、工作区和会话目录的解析方式；
4. 真正的持久配置源、配置加载顺序，以及启动时会被重建的生成文件；
5. Agent 实际发送的协议、Endpoint 拼接规则和模型选择优先级；
6. Direct 与 Proxy 模式的支持边界；
7. 当前会话、新会话、辅助模型、标题模型和工具调用是否使用不同模型别名；
8. 配置生效是否需要退出、重启或刷新会话；
9. 官方模型恢复时需要删除或恢复哪些字段；
10. macOS 与 Windows 的差异是否只属于路径和生命周期层。

设计文档必须说明事实依据、配置示例、受控字段、保留字段、失败恢复、双平台路径和
验收方式。测试脚本不得显式传入本应由 Agent 默认选择的模型，否则不能作为真实
切换成功的证据。

### 6.2 适配器契约

每个新 Agent 必须实现并验证：

1. 稳定、全小写的 Agent ID 和正确显示名称；
2. macOS 与 Windows 安装检测及自定义安装路径；
3. 活跃用户和权威配置路径定位；
4. 配置格式探测和不可写状态说明；
5. Agent 源协议和直连协议约束；
6. 配置生成，并保留未知字段与用户自定义内容；
7. 写后重新读取和精确校验；
8. 第一次接管基线与官方配置恢复；
9. 重复切换幂等，不产生重复 Provider、模型或内部占位项；
10. 运行中安全退出、写入、校验、回滚和按原状态恢复；
11. 前端能力、名称、图标、状态和确认流程；
12. 单元测试、回归测试和真实 Agent 验收。

适配器不得修改安装包内文件，不得向每个会话目录散布配置，不得依赖临时 `.env`
或启动参数注入，除非这是 Agent 官方且唯一的持久配置机制，并在设计中给出证据。

### 6.3 配置写入与恢复

任何 Agent 配置写入必须经过 `ConfigTransaction`：

1. 读取当前文件；
2. 保存不可变原始基线并创建加密操作备份；
3. 校验备份可恢复；
4. 在同目录原子替换目标文件；
5. 重新读取并执行适配器校验；
6. 失败时恢复备份；
7. 只在成功后更新绑定状态。

只修改带明确管理标记的字段。恢复官方配置时，只移除 AT-Switch 管理的内容，并恢复
接管前值；不得覆盖用户在接管后新增的无关配置。

### 6.4 真实验收

新增 Agent 的真实验收至少包括：

- 官方模型切换到第三方模型；
- 第三方模型之间切换；
- 第三方模型恢复官方模型，再次切回第三方模型；
- 全量退出并重新启动 Agent 后配置仍然生效；
- 新建真实会话，不显式指定 Provider 或模型，确认由 Agent 默认配置选中目标模型；
- 从 Agent 日志或上游记录确认真实 Provider、模型和 Endpoint；
- 失败写入或校验失败能够回滚；
- 原有 Agent 的关键回归测试全部通过。

真实请求可能消耗额度或向第三方传输内容时，使用不含敏感信息的唯一测试标记，并遵循
用户已经明确授权的 Provider 与模型范围。

## 7. Provider、协议与本地代理

- 用户保存的 Base URL 是 Provider 根地址，Endpoint 解析必须复用统一语义；
- OpenAI Responses 使用 `/v1/responses`；
- OpenAI Chat Completions 使用 `/v1/chat/completions`；
- Anthropic Messages 使用 `/v1/messages`；
- Base URL 已含协议路径时不得重复拼接；
- Direct 模式写入 Provider Endpoint、真实模型 ID 和必要凭据；
- Proxy 模式写入本地 Endpoint 和本地令牌，由代理完成上游鉴权与协议转换；
- 同协议优先透明转发，不同协议通过 Canonical Request/Response 转换；
- 模型名和 Agent 配置必须使用真实模型 ID，不把内部标识拼进用户可见模型名称；
- Streaming 必须是真流式；Tool 类型、调用 ID、参数和结束原因必须保持语义；
- Codec 改动必须覆盖普通响应、流式响应、Tool 和错误映射测试。

## 8. 安全与隐私

- macOS Secret 使用 Keychain，Windows 使用 Credential Manager；
- SQLite、日志、Toast、前端状态和测试快照不得保存明文 Secret；
- 本地代理只监听 `127.0.0.1`，路由表只保存本地令牌哈希；
- 不持久化 Prompt、回复、Tool 参数或逐条请求日志；
- 错误信息不得包含凭据、请求正文或用户目录中的敏感内容；
- 读取真实本机配置时只输出完成判断所需的字段、哈希或脱敏信息；
- 不修改系统安全设置，不绕过 HTTPS、安全提示或签名检查。

## 9. macOS 与 Windows 一致性

两个平台必须共享注册表、适配器语义、Provider 验证、绑定状态、事务、代理模式和用户
可见错误。平台差异集中在 `locator.rs`、`lifecycle.rs`、系统凭据实现和必要的
`#[cfg(target_os = ...)]` 代码中，不复制两套业务流程。

新增桌面 Agent 必须同时提供 macOS 与 Windows 的发现输入和测试。Windows 安装器应
在 Windows 构建机使用 MSVC 工具链生成；macOS 上因缺少 Windows SDK 导致的交叉编译
失败必须如实记录，不能等同于 Windows 真机测试通过或实现失败。

## 10. 编码规范

### 10.1 Rust

- 必须通过 `cargo fmt` 和 `cargo clippy -D warnings`；
- 使用 `CommandError` 和稳定错误码，不用字符串匹配控制流程；
- 文件系统路径使用 `Path` / `PathBuf`；
- 解析外部配置时兼容缺失字段和未知字段；
- 共享状态使用明确并发原语，不依赖未声明的全局可变状态；
- `unsafe` 仅用于必要系统 API，并在相邻注释说明安全前提。

### 10.2 TypeScript 与 React

- 保持严格类型，不使用无理由的 `any`；
- Props、领域对象和 IPC 返回值必须有明确类型；
- 页面负责编排，复杂转换进入 `src/lib/` 或独立组件；
- 异步操作处理 loading、成功、失败和重复点击；
- `useEffect` 声明正确依赖并清理监听器；
- 浏览器 Mock 与 Tauri 实际返回结构保持一致；
- 使用 Testing Library 从用户视角验证行为，不测试内部实现细节。

## 11. 测试与质量门禁

测试应验证业务契约和失败场景，避免只复刻实现细节。修复缺陷时应增加能够复现根因的
回归测试。可逆且低影响的纯文案修改不强制新增测试。

提交前按影响范围执行，完整门禁为：

```bash
npm run build
npm test -- --run
npm run licenses:check
cargo fmt --manifest-path src-tauri/Cargo.toml -- --check
cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings
cargo test --manifest-path src-tauri/Cargo.toml
```

新增 Agent 还必须覆盖：发现、无配置文件、无效配置、配置生成、未知字段保留、重复切换、
写后校验、接管前基线、官方恢复、协议限制、自动重启和前端确认流程。

CI 必须同时通过 macOS 与 Windows。某个平台无法本地验证时，必须在结果中说明，并在
对应平台或 CI 补测后才能宣称双平台完成。

## 12. Git、提交与发布

- 未经用户明确要求，不执行 `git commit`、`git push`、合并、打标签或发布；
- 用户要求提交时，只暂存当前任务相关文件，提交前执行 `git diff --check` 并检查敏感
  信息、临时文件和构建产物；
- 每个提交聚焦一个问题，避免夹带无关重构或依赖升级；
- 不使用破坏性 Git 命令清理用户工作区；
- 推送前检查当前分支、远端和 GitHub Actions 触发条件；
- 普通代码推送不得附带版本标签，除非用户明确要求发布；
- Pull Request 描述必须说明问题、最终行为、平台影响、数据影响、测试和客观限制；
- 正式发布前必须完成 macOS 与 Windows 真机切换、恢复、重启、安装升级和签名验证。

## 13. 完成定义

一项迭代只有同时满足以下条件才算完成：

- 用户要求的行为已经实现并可复现；
- 根因已解决，没有依赖临时环境修改；
- SOLID 边界清晰，新增能力通过扩展接入；
- 旧 Agent 行为、旧配置和未知字段得到保留；
- 配置写入可校验、失败可回滚、官方状态可恢复；
- macOS 与 Windows 逻辑一致，或平台差异有明确原生原因；
- 有效测试通过，未通过或未执行的检查被准确说明；
- 文档、版本号和代码行为一致；
- 没有 Secret、私有数据、绝对用户路径、临时文件、构建产物或无用代码进入变更；
- 最终汇报准确列出修改、验证结果、工作区状态和客观边界。
